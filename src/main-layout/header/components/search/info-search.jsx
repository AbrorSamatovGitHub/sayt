import React from "react";
const InfoSearch = ({title,id}) => {
  return (
    <div className="container border mb-1 bg-white p-3">
     <p>{title}</p>
    </div>
  );
};

export default InfoSearch;
